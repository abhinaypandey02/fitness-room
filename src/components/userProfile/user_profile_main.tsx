import React from 'react';
import UserInterface from "../../interfaces/user_interface";

function UserProfile({user}:{user:UserInterface}) {
    return (
        <div>

        </div>
    );
}

export default UserProfile;